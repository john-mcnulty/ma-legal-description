{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "ma-registry",
  "displayName": "MA Registry Tools",
  "version": "0.5.9",
  "description": "Finds the vesting deed for a Massachusetts property and extracts a paste-ready legal description from the county registry of deeds.",
  "author": {
    "name": "John McNulty",
    "email": "john.mcnulty80@gmail.com"
  },
  "license": "MIT",
  "keywords": [
    "massachusetts",
    "registry-of-deeds",
    "real-estate",
    "legal-description",
    "title",
    "conveyancing"
  ],
  "userConfig": {
    "output_dir": {
      "type": "directory",
      "title": "Output folder",
      "description": "Where reports, legal-description text files, and downloaded deed PDFs are saved. Runs never write anywhere else.",
      "required": true
    },
    "extraction_mode": {
      "type": "string",
      "title": "Deed text extraction (faster with an Anthropic API key)",
      "description": "RECOMMENDED: set ANTHROPIC_API_KEY in your environment before using the plugin. With a key, the deed's legal description and fields are extracted in ONE API call and the run finishes on its own. Without one, everything still works — but you read the downloaded deed PDFs yourself in-session, which is slower and takes a few manual steps per run.\n\n'auto' (default) needs no decision: it uses the API when the key is present and falls back to in-session reading when it is not. 'claude-code' never calls the API. 'api' forces it and fails with an actionable error if the key is missing.\n\nNo key is required to install or use this plugin. In EVERY mode the registry search, the wrong-parcel guard and the grantor check work without an API key — only the deed's own text extraction differs.",
      "default": "auto"
    },
    "copy_to_clipboard": {
      "type": "boolean",
      "title": "Copy legal description to clipboard",
      "description": "After a successful run, place the paste-ready legal description on the clipboard for pasting into Word or your document system.",
      "default": true
    },
    "write_docx": {
      "type": "boolean",
      "title": "Also write a .docx",
      "description": "Write the legal description as a Word document alongside the .txt. Requires the python-docx package; silently skipped if unavailable.",
      "default": false
    },
    "show_timings": {
      "type": "boolean",
      "title": "Show run timings",
      "description": "Record how long each stage takes and print a timing summary at the end of the report. Timings stay on your machine and are never transmitted.",
      "default": true
    }
  }
}
